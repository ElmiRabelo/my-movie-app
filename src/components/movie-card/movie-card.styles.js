import styled from "styled-components";

export const Container = styled.div`
  height: 265px;
  min-width: 370px;
  max-width: 400px;
  display: inline-grid;
  grid-template-columns: 185px auto;
  overflow: hidden;
  box-shadow: 0 1px 10px rgba(20, 24, 38, 0.9);
  border-radius: 3px;
`;
